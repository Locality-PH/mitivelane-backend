// module.exports = (socket, getUser) => {
	// const sendMessage = (senderAuthToken, receiverAuthToken, message) => {
		// const sendTo = getUser(receiverAuthToken)

		// socket.to(sendTo.socketId).emit("message:receive-message", message)
	// }
	
	// socket.on("message:send-message", sendMessage)
// }

